<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head-links.ejs') -%>
    <title>Hoptimist | <%=brewery.name%></title>
  </head>

  <!-- Nav Bar -->
  <%- include('../partials/navbar.ejs') -%>

  <!-- Hero -->
  <header class="is-family-monospace">
    <section class="hero is-dark">
      <div class="hero-body">
        <div class="container">
          <nav>
            <h2 class="subtitle is-5">Brewery:</h2>
            <h1 class="title is-2"><%=brewery.name%></h1>
          </nav>
        </div>
      </div>
    </section>
  </header>

  <!-- Body -->
  <body>
    <main>
      <br />

      <!-- Update Brewery -->
      <a
        class="content is-link is-medium"
        href="/breweries/<%= brewery._id %>/edit "
        >Update <%=brewery.name %>
      </a>
      <br /><br />

      <!-- Menu -->
      <h3 class="label">All of <%=brewery.name %> beers: <br /></h3>
      <div class="block">
        <aside class="menu">
          <ul class="menu-list">
            <li>
              <a class="subtitle is-active" href="#"
                >See a beer you like?
                <strong class="has-text-white">Check it out. </strong>
              </a>
              <ul>
                <% brewery.beers.forEach(beer => { %>
                <li>
                  <a href="/beers/<%= beer._id %> "><%= beer.name %> </a>
                </li>
                <% }) %>
              </ul>
            </li>
          </ul>
        </aside>
      </div>
      <br /><br />

      <!-- New Beer Button -->
      <h3 class="label">
        Didn't see your beer? <br />
        <a href="/beers/new" class="button is-link">Add New Beer!</a>
      </h3>
    </main>
    <br />
  </body>

  <!-- Footer -->
  <%- include('../partials/footer.ejs') -%>
</html>
